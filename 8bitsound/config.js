// SFX Studio — AI Configuration
// Only needed for Level 3 (AI Assistant). Levels 1 and 2 work without this.
//
// 1. Replace the value below with your OpenAI API key.
// 2. Recommended: restrict the key to your domain in platform.openai.com/api-keys
// 3. Set a spending limit so it can't run up a large bill.
//
window.OPENAI_API_KEY = 'your-openai-api-key-here';
